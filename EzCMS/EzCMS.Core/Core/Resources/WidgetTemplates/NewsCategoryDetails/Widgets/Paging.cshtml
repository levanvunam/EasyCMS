@if (Model.NewsListing != null && Model.NewsListing.PageSize > 0 && Model.NewsListing.NewsListing.Any())
{
    <div class="blog-page-navigation-wrap">
        <ul>
            <li class="prev">
                <a href="?page=@Model.NewsListing.PreviousPage">
                    <span class="fa fa-angle-left"></span>Prev
                </a>
            </li>
            @for (int i = 0; i < Model.NewsListing.TotalPage; i++)
            {
                <li class="@(Model.NewsListing.PageIndex == i ? "current" : "")">
                    <a href="?page=@i">@(i + 1)</a>
                </li>

            }
            <li class="next">
                <a href="?page=@Model.NewsListing.NextPage">
                    Next
                    <span class="fa fa-angle-right"></span>
                </a>
            </li>
        </ul>
    </div>
    <div class="clearfix"></div>
}