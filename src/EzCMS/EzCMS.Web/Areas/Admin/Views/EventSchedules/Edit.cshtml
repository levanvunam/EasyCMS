@model EzCMS.Core.Models.EventSchedules.EventScheduleManageModel

@{
    ViewBag.Header = T("EventSchedule_Site_Header");
    ViewBag.Title = T("EventSchedule_Site_EditTitle");
    ViewBag.Description = T("EventSchedule_Site_EditDescription");
}

<div class="page-header">
    <h1>
        @ViewBag.Header
        <small>
            <i class="fa fa-angle-double-right"></i>
            @ViewBag.Title
        </small>
    </h1>
</div>

<div class="row">
    <div class="col-xs-12">
        @ShowStatusMessage()
        @using (Html.BeginForm("Edit", "EventSchedules", null, FormMethod.Post, new { id = "event-schedule-form", @class = "form-horizontal" }))
        {
            @Html.EditorForModel(Model)
        }
    </div>
</div>